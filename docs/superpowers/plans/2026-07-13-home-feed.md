# 홈 피드 (3컬럼) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** LinkedIn 스타일 3컬럼 "홈" 화면(`/home`)과 이를 지탱하는 백엔드 API 2개(feed, news)를 만든다.

**Architecture:** 백엔드는 신규 라우터 2개(`feed.py`, `news.py`)를 `/api/v1`에 추가한다. feed는 기존 posting CRUD 헬퍼를 재사용하고 옵셔널 인증으로 매치 개인화를 얹는다. news는 외부 3소스(HN Algolia, GeekNews RSS, GitHub Search API)를 requests로 fetch해 Redis TTL 4h lazy 캐시(+24h stale 폴백)한다. DB 저장 없음. 프론트는 `src/desktop/pages/home/`에 신규 페이지를 만들고 사이드바에 "홈" 섹션을 추가한다.

**Tech Stack:** FastAPI + SQLAlchemy/SQLModel + Redis(sync client) + requests / React + Vite + react-router + lucide-react. 프론트 시각 값은 Opus 디자인 스펙을 따른다.

**스펙:** `docs/superpowers/specs/2026-07-13-home-feed-design.md`
**비주얼 스펙(디자인 산출물):** `docs/superpowers/specs/2026-07-13-home-feed-visual.md` + `2026-07-13-home-feed-mockup.html` — 프론트 태스크의 CSS 클래스명/색/간격/상태 값은 이 문서가 유일한 출처다.

## Global Constraints

- 백엔드 repo: `/home/rivermoon/Documents/techeer-2026-summer-a/backend` (브랜치 main).
- 프론트 repo: `/home/rivermoon/Documents/techeer-2026-summer-a/frontend` (브랜치 main). 별개 git repo.
- **커밋 정책 (중요):** 두 repo 모두 이 작업 이전부터 uncommitted 변경이 있다.
  - 절대 `git add -A` / `git add .` 금지. 커밋할 파일을 항상 명시적으로 나열한다.
  - 이 플랜이 **새로 만드는 파일**과 backend `app/main.py`(현재 clean)만 커밋한다.
  - frontend `src/App.tsx`, `src/desktop/DesktopShell.tsx`는 이미 dirty이므로 **수정만 하고 커밋하지 않는다** (최종 보고에 명시).
  - 기존 dirty 파일(backend: `app/routers/chat.py`, `app/routers/resume.py`, `app/services/rag/*`, `app/core/config.py`, `.env.example`, `app/services/resume_feedback.py` / frontend: `src/rag/*`, `src/pages/Gallery.tsx`)은 절대 건드리지도 커밋하지도 않는다.
- 새 런타임 의존성 추가 금지. 뉴스 fetch는 `requests`(이미 runtime dep), RSS 파싱은 stdlib `xml.etree.ElementTree`.
- 코드/주석에 이모지 금지. UI 카피는 한국어.
- 모든 신규 API는 `/api/v1` 프리픽스로 등록.
- 백엔드 테스트: 모듈별 self-contained 픽스처(in-memory sqlite + `StaticPool`) — `tests/test_postings.py` 패턴을 따른다. conftest 없음.
- 프론트 자동 테스트 인프라 없음 — 검증은 `npm run build`(타입체크) + Playwright(별도 태스크).
- 실행 커맨드: 백엔드 테스트 `cd backend && python -m pytest tests/test_feed.py -v` 형식. 프론트 빌드 `cd frontend && npm run build`.

---

## Task B1: 뉴스 API (`GET /api/v1/news`)

**Files:**
- Create: `backend/app/schemas/news.py`
- Create: `backend/app/services/news.py`
- Create: `backend/app/routers/news.py`
- Modify: `backend/app/main.py` (라우터 등록 2줄)
- Test: `backend/tests/test_news.py`

**Interfaces:**
- Consumes: `app.core.redis.redis_client` (sync, `decode_responses=True`)
- Produces: `GET /api/v1/news?source=hackernews|geeknews|github&limit=15` → `NewsResponse { source, items: NewsItem[], fetched_at: str(ISO), stale: bool, error: bool }`. `NewsItem { title, url, comments_url?, points?, comments_count?, language?, stars?, description? }`. 프론트 Task F1이 이 형태를 그대로 타입으로 옮긴다.

- [ ] **Step 1: 실패하는 테스트 작성** — `backend/tests/test_news.py`

```python
import json

from fastapi.testclient import TestClient

from app.main import app


class FakeRedis:
    def __init__(self):
        self.store: dict[str, str] = {}

    def get(self, key: str):
        return self.store.get(key)

    def setex(self, key: str, ttl: int, value: str):
        self.store[key] = value


HN_ITEMS = [
    {
        "title": "Show HN: Test",
        "url": "https://example.com/a",
        "comments_url": "https://news.ycombinator.com/item?id=1",
        "points": 120,
        "comments_count": 45,
    }
]


def _patch(monkeypatch, fake_redis, fetcher):
    from app.services import news as news_service

    monkeypatch.setattr(news_service, "redis_client", fake_redis)
    monkeypatch.setitem(news_service._FETCHERS, "hackernews", fetcher)
    return news_service


def test_cache_miss_fetches_and_caches(monkeypatch):
    fake = FakeRedis()
    _patch(monkeypatch, fake, lambda limit: HN_ITEMS)
    client = TestClient(app)

    res = client.get("/api/v1/news", params={"source": "hackernews", "limit": 5})

    assert res.status_code == 200
    body = res.json()
    assert body["source"] == "hackernews"
    assert body["error"] is False and body["stale"] is False
    assert body["items"][0]["title"] == "Show HN: Test"
    assert "news:hackernews" in fake.store
    assert "news:hackernews:stale" in fake.store


def test_cache_hit_does_not_fetch(monkeypatch):
    fake = FakeRedis()
    payload = {
        "source": "hackernews",
        "items": HN_ITEMS,
        "fetched_at": "2026-07-13T00:00:00+00:00",
        "stale": False,
        "error": False,
    }
    fake.store["news:hackernews"] = json.dumps(payload, ensure_ascii=False)

    def boom(limit):
        raise AssertionError("must not fetch on cache hit")

    _patch(monkeypatch, fake, boom)
    client = TestClient(app)

    res = client.get("/api/v1/news", params={"source": "hackernews"})

    assert res.status_code == 200
    assert res.json()["fetched_at"] == "2026-07-13T00:00:00+00:00"


def test_fetch_failure_falls_back_to_stale(monkeypatch):
    fake = FakeRedis()
    payload = {
        "source": "hackernews",
        "items": HN_ITEMS,
        "fetched_at": "2026-07-12T00:00:00+00:00",
        "stale": False,
        "error": False,
    }
    fake.store["news:hackernews:stale"] = json.dumps(payload, ensure_ascii=False)

    def boom(limit):
        raise RuntimeError("network down")

    _patch(monkeypatch, fake, boom)
    client = TestClient(app)

    res = client.get("/api/v1/news", params={"source": "hackernews"})

    assert res.status_code == 200
    body = res.json()
    assert body["stale"] is True and body["error"] is False
    assert body["items"][0]["title"] == "Show HN: Test"


def test_fetch_failure_without_stale_returns_error_flag(monkeypatch):
    fake = FakeRedis()

    def boom(limit):
        raise RuntimeError("network down")

    _patch(monkeypatch, fake, boom)
    client = TestClient(app)

    res = client.get("/api/v1/news", params={"source": "hackernews"})

    assert res.status_code == 200
    body = res.json()
    assert body["error"] is True and body["items"] == []


def test_limit_slices_cached_items(monkeypatch):
    fake = FakeRedis()
    many = [dict(HN_ITEMS[0], title=f"t{i}") for i in range(10)]
    fake.store["news:hackernews"] = json.dumps(
        {
            "source": "hackernews",
            "items": many,
            "fetched_at": "2026-07-13T00:00:00+00:00",
            "stale": False,
            "error": False,
        },
        ensure_ascii=False,
    )
    _patch(monkeypatch, fake, lambda limit: many)
    client = TestClient(app)

    res = client.get("/api/v1/news", params={"source": "hackernews", "limit": 3})

    assert len(res.json()["items"]) == 3


def test_invalid_source_rejected():
    client = TestClient(app)
    res = client.get("/api/v1/news", params={"source": "reddit"})
    assert res.status_code == 422
```

- [ ] **Step 2: 실패 확인**

Run: `cd /home/rivermoon/Documents/techeer-2026-summer-a/backend && python -m pytest tests/test_news.py -v`
Expected: FAIL (`ModuleNotFoundError: app.services.news` 또는 404)

- [ ] **Step 3: 스키마 작성** — `backend/app/schemas/news.py`

```python
from typing import Literal

from pydantic import BaseModel

NewsSource = Literal["hackernews", "geeknews", "github"]


class NewsItem(BaseModel):
    title: str
    url: str
    comments_url: str | None = None
    points: int | None = None
    comments_count: int | None = None
    language: str | None = None
    stars: int | None = None
    description: str | None = None


class NewsResponse(BaseModel):
    source: NewsSource
    items: list[NewsItem]
    fetched_at: str
    stale: bool = False
    error: bool = False
```

- [ ] **Step 4: 서비스 작성** — `backend/app/services/news.py`

```python
import json
import xml.etree.ElementTree as ET
from datetime import datetime, timedelta, timezone

import requests

from app.core.redis import redis_client

NEWS_CACHE_TTL_SECONDS = 4 * 60 * 60
NEWS_STALE_TTL_SECONDS = 24 * 60 * 60
NEWS_FETCH_LIMIT = 30
_FETCH_TIMEOUT_SECONDS = 8


def _now_iso() -> str:
    return datetime.now(timezone.utc).isoformat()


def _fetch_hackernews(limit: int) -> list[dict]:
    response = requests.get(
        "https://hn.algolia.com/api/v1/search",
        params={"tags": "front_page", "hitsPerPage": limit},
        timeout=_FETCH_TIMEOUT_SECONDS,
    )
    response.raise_for_status()
    items: list[dict] = []
    for hit in response.json().get("hits", [])[:limit]:
        thread_url = f"https://news.ycombinator.com/item?id={hit['objectID']}"
        items.append(
            {
                "title": hit.get("title") or "(제목 없음)",
                "url": hit.get("url") or thread_url,
                "comments_url": thread_url,
                "points": hit.get("points"),
                "comments_count": hit.get("num_comments"),
            }
        )
    return items


def _fetch_geeknews(limit: int) -> list[dict]:
    response = requests.get("https://news.hada.io/rss/news", timeout=_FETCH_TIMEOUT_SECONDS)
    response.raise_for_status()
    root = ET.fromstring(response.content)
    items: list[dict] = []
    for item in root.iter("item"):
        title = item.findtext("title")
        link = item.findtext("link")
        if not title or not link:
            continue
        items.append({"title": title, "url": link, "comments_url": link})
        if len(items) >= limit:
            break
    return items


def _fetch_github(limit: int) -> list[dict]:
    created_after = (datetime.now(timezone.utc) - timedelta(days=7)).date().isoformat()
    response = requests.get(
        "https://api.github.com/search/repositories",
        params={
            "q": f"created:>{created_after}",
            "sort": "stars",
            "order": "desc",
            "per_page": limit,
        },
        headers={"Accept": "application/vnd.github+json"},
        timeout=_FETCH_TIMEOUT_SECONDS,
    )
    response.raise_for_status()
    items: list[dict] = []
    for repo in response.json().get("items", [])[:limit]:
        items.append(
            {
                "title": repo["full_name"],
                "url": repo["html_url"],
                "description": repo.get("description"),
                "language": repo.get("language"),
                "stars": repo.get("stargazers_count"),
            }
        )
    return items


_FETCHERS = {
    "hackernews": _fetch_hackernews,
    "geeknews": _fetch_geeknews,
    "github": _fetch_github,
}


def get_news(source: str, limit: int) -> dict:
    cache_key = f"news:{source}"
    cached = redis_client.get(cache_key)
    if cached is not None:
        payload = json.loads(cached)
        payload["items"] = payload["items"][:limit]
        return payload

    try:
        items = _FETCHERS[source](NEWS_FETCH_LIMIT)
    except Exception:
        stale_raw = redis_client.get(f"{cache_key}:stale")
        if stale_raw is not None:
            payload = json.loads(stale_raw)
            payload["items"] = payload["items"][:limit]
            payload["stale"] = True
            return payload
        return {
            "source": source,
            "items": [],
            "fetched_at": _now_iso(),
            "stale": False,
            "error": True,
        }

    payload = {
        "source": source,
        "items": items,
        "fetched_at": _now_iso(),
        "stale": False,
        "error": False,
    }
    body = json.dumps(payload, ensure_ascii=False)
    redis_client.setex(cache_key, NEWS_CACHE_TTL_SECONDS, body)
    redis_client.setex(f"{cache_key}:stale", NEWS_STALE_TTL_SECONDS, body)
    payload["items"] = payload["items"][:limit]
    return payload
```

- [ ] **Step 5: 라우터 작성** — `backend/app/routers/news.py`

```python
from typing import Annotated

from fastapi import APIRouter, Query

from app.schemas.news import NewsResponse, NewsSource
from app.services.news import get_news

router = APIRouter()


@router.get("/news", response_model=NewsResponse)
def read_news(
    source: Annotated[NewsSource, Query(description="hackernews | geeknews | github")],
    limit: Annotated[int, Query(ge=1, le=30)] = 15,
) -> NewsResponse:
    """기술 뉴스 피드. Redis 4시간 캐시, 실패 시 24시간 stale 폴백."""
    return NewsResponse(**get_news(source, limit))
```

- [ ] **Step 6: 라우터 등록** — `backend/app/main.py`

기존 import 블록의 다른 라우터 import와 같은 스타일로 news 라우터를 import하고(파일 상단의 기존 라우터 import 형식을 그대로 따를 것), `app.include_router(search_router, ...)` 근처에 추가:

```python
app.include_router(news_router, prefix="/api/v1", tags=["news"])
```

- [ ] **Step 7: 테스트 통과 확인**

Run: `cd /home/rivermoon/Documents/techeer-2026-summer-a/backend && python -m pytest tests/test_news.py -v`
Expected: 6 PASS

- [ ] **Step 8: 전체 테스트 회귀 확인**

Run: `cd /home/rivermoon/Documents/techeer-2026-summer-a/backend && python -m pytest tests/ -q`
Expected: 기존 테스트 포함 전부 PASS

- [ ] **Step 9: 커밋 (명시적 파일만)**

```bash
cd /home/rivermoon/Documents/techeer-2026-summer-a/backend
git add app/schemas/news.py app/services/news.py app/routers/news.py app/main.py tests/test_news.py
git commit -m "feat(news): 기술 뉴스 API 추가 (HN/GeekNews/GitHub, Redis TTL 4h lazy 캐시)"
```

---

## Task B2: 피드 API (`GET /api/v1/feed/postings`)

**Files:**
- Create: `backend/app/schemas/feed.py`
- Create: `backend/app/crud/feed.py`
- Create: `backend/app/routers/feed.py`
- Modify: `backend/app/main.py` (라우터 등록)
- Test: `backend/tests/test_feed.py`

**Interfaces:**
- Consumes: `app.routers.match.get_user_from_optional_authorization(session, authorization) -> User | None`, `app.services.match.get_skill_ids_from_resume(session, resume_id, current_user) -> set[int]`, `app.crud.posting`의 `_count_filtered_postings` / `_get_filtered_postings`(limit/offset 지원, latest 정렬) / `_get_posting_urls` / `_format_region` — **호출 전 반드시 `app/crud/posting.py:33-138`의 `list_posting_cards`를 읽고 실제 시그니처에 맞춰 호출한다.**
- Produces: `GET /api/v1/feed/postings?page=1&page_size=20&pool=domestic&category=백엔드` → `FeedResponse { items: FeedPostingItem[], page, page_size, total, as_of }`. `FeedPostingItem { id, title, company?, industry?, region?, pool?, post_date?, close_date?, categories: [str], skills: [str], url, match?: { rate: float(0~100, 소수 1자리), owned_skills: [str], missing_skills: [str] } }`. 프론트 Task F1이 그대로 타입으로 옮긴다.

- [ ] **Step 1: 실패하는 테스트 작성** — `backend/tests/test_feed.py`

`tests/test_postings.py`의 client 픽스처 패턴(in-memory sqlite + `StaticPool` + `Base.metadata.create_all` + `app.dependency_overrides[get_session]`)을 복제해 다음 데이터를 시드한다: Skill 3개(canonical: `python`, `react`, `aws`), User 1명(id=1), Resume 1개(user_id=1) + ResumeSkill(python, react), Posting 3개 —
p1(pool=domestic, industry="IT서비스", region_city="서울", post_date=오늘, close_date=오늘+10, techs=[python, aws], categories=["백엔드"]),
p2(pool=domestic, post_date=어제, techs=[react], categories=["프론트엔드"]),
p3(pool=global, post_date=3일 전, techs=[], categories=[]). 각각 RawPosting(payload={"url": ...}) 포함.

```python
def test_feed_anonymous_returns_cards_without_match(client):
    res = client.get("/api/v1/feed/postings", params={"page": 1, "page_size": 20})
    assert res.status_code == 200
    body = res.json()
    assert body["total"] == 3
    first = body["items"][0]
    assert first["title"] == "p1 title"          # post_date 내림차순
    assert first["industry"] == "IT서비스"
    assert first["region"] == "서울"
    assert first["categories"] == ["백엔드"]
    assert sorted(first["skills"]) == ["aws", "python"]
    assert first["match"] is None


def test_feed_authed_includes_match(client, monkeypatch):
    monkeypatch.setattr("app.routers.match.is_token_blocklisted", lambda token: False)
    from app.core.security import create_access_token
    token = create_access_token(1)
    res = client.get(
        "/api/v1/feed/postings",
        headers={"Authorization": f"Bearer {token}"},
    )
    assert res.status_code == 200
    first = res.json()["items"][0]               # p1: skills=[python, aws], 보유=[python, react]
    assert first["match"]["rate"] == 50.0
    assert first["match"]["owned_skills"] == ["python"]
    assert first["match"]["missing_skills"] == ["aws"]


def test_feed_posting_without_skills_has_null_match_when_authed(client, monkeypatch):
    monkeypatch.setattr("app.routers.match.is_token_blocklisted", lambda token: False)
    from app.core.security import create_access_token
    token = create_access_token(1)
    res = client.get("/api/v1/feed/postings", headers={"Authorization": f"Bearer {token}"})
    p3 = [i for i in res.json()["items"] if i["skills"] == []][0]
    assert p3["match"] is None


def test_feed_pool_filter(client):
    res = client.get("/api/v1/feed/postings", params={"pool": "global"})
    assert res.json()["total"] == 1


def test_feed_category_filter(client):
    res = client.get("/api/v1/feed/postings", params={"category": "백엔드"})
    body = res.json()
    assert body["total"] == 1
    assert body["items"][0]["categories"] == ["백엔드"]


def test_feed_pagination(client):
    res = client.get("/api/v1/feed/postings", params={"page": 2, "page_size": 1})
    body = res.json()
    assert body["total"] == 3
    assert len(body["items"]) == 1
    assert body["page"] == 2
```

- [ ] **Step 2: 실패 확인**

Run: `cd /home/rivermoon/Documents/techeer-2026-summer-a/backend && python -m pytest tests/test_feed.py -v`
Expected: FAIL (404)

- [ ] **Step 3: 스키마 작성** — `backend/app/schemas/feed.py`

```python
from datetime import date

from pydantic import BaseModel

from app.schemas.posting import Pool


class FeedMatch(BaseModel):
    rate: float
    owned_skills: list[str]
    missing_skills: list[str]


class FeedPostingItem(BaseModel):
    id: int
    title: str
    company: str | None
    industry: str | None
    region: str | None
    pool: str | None
    post_date: date | None
    close_date: date | None
    categories: list[str]
    skills: list[str]
    url: str
    match: FeedMatch | None = None


class FeedResponse(BaseModel):
    items: list[FeedPostingItem]
    page: int
    page_size: int
    total: int
    as_of: str
```

- [ ] **Step 4: CRUD 작성** — `backend/app/crud/feed.py`

`app/crud/posting.py`를 먼저 읽고 헬퍼 시그니처를 확인한 뒤 작성한다. 핵심 구조:

```python
from sqlalchemy import select

from app.crud.posting import (
    _count_filtered_postings,
    _get_filtered_postings,
    _get_posting_urls,
    _format_region,
)
from app.models.posting import PostingCategory, PostingTech
from app.models.skill import Skill
from app.schemas.feed import FeedMatch, FeedPostingItem


def _get_feed_skills(session, posting_ids: list[int]) -> dict[int, list[tuple[int, str]]]:
    """posting_id -> [(skill_id, canonical), ...] (canonical 오름차순)"""
    if not posting_ids:
        return {}
    rows = session.execute(
        select(PostingTech.posting_id, Skill.id, Skill.canonical)
        .join(Skill, Skill.id == PostingTech.skill_id)
        .where(PostingTech.posting_id.in_(posting_ids))
        .order_by(Skill.canonical)
    ).all()
    out: dict[int, list[tuple[int, str]]] = {}
    for posting_id, skill_id, canonical in rows:
        out.setdefault(posting_id, []).append((skill_id, canonical))
    return out


def _get_feed_categories(session, posting_ids: list[int]) -> dict[int, list[str]]:
    if not posting_ids:
        return {}
    rows = session.execute(
        select(PostingCategory.posting_id, PostingCategory.category)
        .where(PostingCategory.posting_id.in_(posting_ids))
        .order_by(PostingCategory.category)
    ).all()
    out: dict[int, list[str]] = {}
    for posting_id, category in rows:
        out.setdefault(posting_id, []).append(category)
    return out


def _build_match(
    skills: list[tuple[int, str]], owned_skill_ids: set[int] | None
) -> FeedMatch | None:
    if owned_skill_ids is None or not skills:
        return None
    owned = [name for skill_id, name in skills if skill_id in owned_skill_ids]
    missing = [name for skill_id, name in skills if skill_id not in owned_skill_ids]
    rate = round(100 * len(owned) / len(skills), 1)
    return FeedMatch(rate=rate, owned_skills=owned, missing_skills=missing)


def list_feed_postings(
    *,
    session,
    pool: str | None,
    category: str | None,
    page: int,
    page_size: int,
    owned_skill_ids: set[int] | None,
) -> tuple[list[FeedPostingItem], int]:
    total = _count_filtered_postings(session=session, pool=pool, position=category)
    postings = _get_filtered_postings(
        session=session,
        pool=pool,
        position=category,
        sort="latest",
        limit=page_size,
        offset=(page - 1) * page_size,
    )
    ids = [p.id for p in postings]
    skills_map = _get_feed_skills(session, ids)
    categories_map = _get_feed_categories(session, ids)
    urls = _get_posting_urls(session, ids)

    items: list[FeedPostingItem] = []
    for p in postings:
        skills = skills_map.get(p.id, [])
        items.append(
            FeedPostingItem(
                id=p.id,
                title=p.title,
                company=p.company,
                industry=p.industry,
                region=_format_region(p),
                pool=p.pool,
                post_date=p.post_date,
                close_date=p.close_date,
                categories=categories_map.get(p.id, []),
                skills=[name for _, name in skills],
                url=urls.get(p.id, ""),
                match=_build_match(skills, owned_skill_ids),
            )
        )
    return items, total
```

주의: `_count_filtered_postings`/`_get_filtered_postings`/`_get_posting_urls`/`_format_region`의 실제 파라미터명·반환형이 위와 다르면 **feed.py 쪽을 실제 시그니처에 맞춘다** (posting.py를 수정하지 않는다). 모델 import 경로(`app.models.posting`, `app.models.skill`)도 실제 파일 기준으로 확인.

- [ ] **Step 5: 라우터 작성** — `backend/app/routers/feed.py`

```python
from datetime import date
from typing import Annotated

from fastapi import APIRouter, Header, Query
from sqlalchemy import select

from app.core.deps import SessionDep
from app.crud.feed import list_feed_postings
from app.models.resume import Resume
from app.routers.match import get_user_from_optional_authorization
from app.schemas.feed import FeedResponse
from app.schemas.posting import Pool
from app.services.match import get_skill_ids_from_resume

router = APIRouter()


def _resolve_owned_skill_ids_for_user(session, user) -> set[int] | None:
    """유저의 대표(최신) 이력서 스킬셋. 이력서가 없거나 실패하면 None (익명 degrade)."""
    try:
        resume_id = session.execute(
            select(Resume.id)
            .where(Resume.user_id == user.id, Resume.is_deleted == False)  # noqa: E712
            .order_by(Resume.id.desc())
            .limit(1)
        ).scalar_one_or_none()
        if resume_id is None:
            return None
        return get_skill_ids_from_resume(
            session=session, resume_id=resume_id, current_user=user
        )
    except Exception:
        return None


@router.get("/feed/postings", response_model=FeedResponse)
def read_feed_postings(
    session: SessionDep,
    pool: Annotated[Pool | None, Query()] = None,
    category: Annotated[str | None, Query(description="job_category name")] = None,
    page: Annotated[int, Query(ge=1)] = 1,
    page_size: Annotated[int, Query(ge=1, le=50)] = 20,
    authorization: Annotated[str | None, Header()] = None,
) -> FeedResponse:
    """홈 피드용 공고 타임라인 (최신순). 로그인 시 매치 개인화 포함."""
    user = get_user_from_optional_authorization(session, authorization)
    owned_skill_ids = (
        _resolve_owned_skill_ids_for_user(session, user) if user is not None else None
    )
    items, total = list_feed_postings(
        session=session,
        pool=pool,
        category=category,
        page=page,
        page_size=page_size,
        owned_skill_ids=owned_skill_ids,
    )
    return FeedResponse(
        items=items,
        page=page,
        page_size=page_size,
        total=total,
        as_of=date.today().isoformat(),
    )
```

주의: `Resume` 모델의 소프트삭제 필드명(`is_deleted`)과 유저 FK 필드명(`user_id`)은 `app/models/resume.py`에서 확인 후 맞춘다.

- [ ] **Step 6: 라우터 등록** — `backend/app/main.py`

```python
app.include_router(feed_router, prefix="/api/v1", tags=["feed"])
```

(import는 기존 스타일을 따른다.)

- [ ] **Step 7: 테스트 통과 확인**

Run: `cd /home/rivermoon/Documents/techeer-2026-summer-a/backend && python -m pytest tests/test_feed.py -v`
Expected: 6 PASS

- [ ] **Step 8: 전체 테스트 회귀 확인**

Run: `cd /home/rivermoon/Documents/techeer-2026-summer-a/backend && python -m pytest tests/ -q`
Expected: 전부 PASS

- [ ] **Step 9: 커밋**

```bash
cd /home/rivermoon/Documents/techeer-2026-summer-a/backend
git add app/schemas/feed.py app/crud/feed.py app/routers/feed.py app/main.py tests/test_feed.py
git commit -m "feat(feed): 홈 피드 공고 API 추가 (옵셔널 인증 매치 개인화, DB 페이지네이션)"
```

---

## Task F1: 홈 API 클라이언트 (`homeApi.ts`)

**Files:**
- Create: `frontend/src/career/homeApi.ts`

**Interfaces:**
- Consumes: `getAuthToken` from `./authStore`; 백엔드 B1/B2 응답 형태.
- Produces: `homeApi.feed(params) -> Promise<FeedResponseDto>`, `homeApi.news(source, limit?) -> Promise<NewsResponseDto>`, `homeApi.postingTimeline(pool, days?, resumeId?) -> Promise<PostingTimelineDto>`, `homeApi.skillShare(pool, topK?) -> Promise<SkillShareDto>`. 타입 `FeedPostingDto`, `FeedMatchDto`, `NewsItemDto`, `NewsSource` export — Task F3이 소비.

- [ ] **Step 1: 파일 작성** — `frontend/src/career/homeApi.ts` (`recruitmentApi.ts`의 내부 토큰 주입 패턴을 따른다)

```ts
import { getAuthToken } from './authStore'

const API_BASE = '/api/v1'

export type NewsSource = 'hackernews' | 'geeknews' | 'github'

export type FeedMatchDto = {
  rate: number
  owned_skills: string[]
  missing_skills: string[]
}

export type FeedPostingDto = {
  id: number
  title: string
  company: string | null
  industry: string | null
  region: string | null
  pool: string | null
  post_date: string | null
  close_date: string | null
  categories: string[]
  skills: string[]
  url: string
  match: FeedMatchDto | null
}

export type FeedResponseDto = {
  items: FeedPostingDto[]
  page: number
  page_size: number
  total: number
  as_of: string
}

export type NewsItemDto = {
  title: string
  url: string
  comments_url: string | null
  points: number | null
  comments_count: number | null
  language: string | null
  stars: number | null
  description: string | null
}

export type NewsResponseDto = {
  source: NewsSource
  items: NewsItemDto[]
  fetched_at: string
  stale: boolean
  error: boolean
}

export type PostingTimelineDto = {
  daily: { date: string; total: number; matched?: number | null }[]
  as_of: string
}

export type SkillShareDto = {
  items: { skill: string; posting_count: number; share: number }[]
  as_of: string
  sample_size: number
}

function query(path: string, params: Record<string, string | number | boolean | undefined>) {
  const usp = new URLSearchParams()
  for (const [key, value] of Object.entries(params)) {
    if (value !== undefined) usp.set(key, String(value))
  }
  const qs = usp.toString()
  return `${API_BASE}${path}${qs ? `?${qs}` : ''}`
}

async function get<T>(path: string, params: Record<string, string | number | boolean | undefined> = {}) {
  const token = getAuthToken()
  const response = await fetch(query(path, params), {
    headers: token ? { Authorization: `Bearer ${token}` } : undefined,
  })
  if (!response.ok) {
    const body = await response.json().catch(() => null)
    throw new Error(typeof body?.detail === 'string' ? body.detail : '데이터를 불러오지 못했습니다.')
  }
  return response.json() as Promise<T>
}

export const homeApi = {
  feed: (params: { page?: number; page_size?: number; pool?: string; category?: string } = {}) =>
    get<FeedResponseDto>('/feed/postings', params),
  news: (source: NewsSource, limit = 15) => get<NewsResponseDto>('/news', { source, limit }),
  postingTimeline: (pool: string, days = 7, resumeId?: number) =>
    get<PostingTimelineDto>('/stats/posting-timeline', { pool, days, resume_id: resumeId }),
  skillShare: (pool: string, topK = 3) =>
    get<SkillShareDto>('/stats/skill-share', { pool, top_k: topK }),
}
```

주의: `SkillShareDto`의 item 필드명은 `app/schemas/insight.py`의 `SkillShareResponse` 실제 필드에 맞춘다 (작성 전 확인).

- [ ] **Step 2: 타입체크**

Run: `cd /home/rivermoon/Documents/techeer-2026-summer-a/frontend && npm run build`
Expected: 성공 (미사용 export 경고는 무시)

- [ ] **Step 3: 커밋**

```bash
cd /home/rivermoon/Documents/techeer-2026-summer-a/frontend
git add src/career/homeApi.ts
git commit -m "feat(home): 홈 피드/뉴스 API 클라이언트 추가"
```

---

## Task F2: 라우트 + 사이드바 "홈" 메뉴

**Files:**
- Modify: `frontend/src/App.tsx` (**dirty — 수정만, 커밋 금지**)
- Modify: `frontend/src/desktop/DesktopShell.tsx` (**dirty — 수정만, 커밋 금지**)

**Interfaces:**
- Consumes: Task F3의 `DesktopHome` (default export, `src/desktop/pages/home/DesktopHome.tsx`). F3보다 먼저 실행한다면 임시로 `<div>홈</div>` 스텁을 같은 경로에 만들지 말고, F3와 같은 서브에이전트 세션에서 함께 처리한다.
- Produces: `/home` 라우트, 사이드바 최상단 "홈" 섹션.

- [ ] **Step 1: App.tsx에 라우트 추가**

import 추가:

```tsx
import DesktopHome from './desktop/pages/home/DesktopHome'
```

`ResponsiveProductLayout` 라우트 그룹의 `path="/"` 라인 바로 아래에 추가 (Adaptive 없이 — DesktopHome이 자체 반응형):

```tsx
<Route path="/home" element={<DesktopHome />} />
```

- [ ] **Step 2: DesktopShell.tsx SECTIONS 최상단에 홈 섹션 추가**

lucide-react import에 `Home` 추가. SECTIONS 배열의 `dash` 섹션 **앞에**:

```tsx
{
  key: 'home',
  label: '홈',
  icon: Home,
  home: '/home',
  match: ['/home'],
  items: [{ to: '/home', label: '피드', end: true }],
},
```

`sectionOf()`는 `pathname === '/'`만 대시보드로 특별 처리하므로 `/home`은 prefix 매칭으로 자동 인식된다. 단, `SECTIONS[0]`이 홈으로 바뀌므로 `sectionOf`의 `if (pathname === '/') return SECTIONS[0]`이 잘못된 섹션을 반환하지 않는지 확인하고, 필요하면 `SECTIONS.find((s) => s.key === 'dash')!`로 수정한다.

- [ ] **Step 3: 수동 확인**

Run: `cd /home/rivermoon/Documents/techeer-2026-summer-a/frontend && npm run build`
Expected: 성공. (커밋하지 않는다 — 두 파일은 기존 dirty 상태)

---

## Task F3: 홈 화면 컴포넌트 (3컬럼)

**Files:**
- Create: `frontend/src/desktop/pages/home/DesktopHome.tsx`
- Create: `frontend/src/desktop/pages/home/HomeLeftColumn.tsx`
- Create: `frontend/src/desktop/pages/home/HomeFeedCard.tsx`
- Create: `frontend/src/desktop/pages/home/HomeNewsPanel.tsx`
- Create: `frontend/src/desktop/pages/home/DesktopHome.css`

**Interfaces:**
- Consumes: `homeApi` + DTO 타입 (F1), `useAuth`/`getAuthToken` (`career/authStore`), `useBookmarks`/`toggleBookmark`/`isBookmarked` (`career/bookmarkStore`, id는 `String(posting.id)`), `recordView` (`career/viewHistoryStore`), `ddayInfo` (`career/state`), `ErrorState`/`EmptyState` (`career/states`), `useResumesState` (`career/state` — 좌측 이력서 요약).
- Produces: `DesktopHome` default export. **모든 시각 값(클래스명, 색, 간격, 상태, 스켈레톤, 카피)은 `docs/superpowers/specs/2026-07-13-home-feed-visual.md`를 그대로 따른다. 목업 `2026-07-13-home-feed-mockup.html`을 브라우저로 열어 대조한다.**

- [ ] **Step 1: DesktopHome.tsx — 레이아웃 + 데이터 오케스트레이션**

구조 (시각 값은 비주얼 스펙 기준):

```tsx
import { useCallback, useEffect, useMemo, useRef, useState } from 'react'
import { useAuth } from '../../../career/authStore'
import { homeApi, type FeedPostingDto } from '../../../career/homeApi'
import HomeLeftColumn from './HomeLeftColumn'
import HomeFeedCard from './HomeFeedCard'
import HomeNewsPanel from './HomeNewsPanel'
import { ErrorState, EmptyState } from '../../../career/states'
import './DesktopHome.css'

const PAGE_SIZE = 20

export default function DesktopHome() {
  const { user, isAuthed } = useAuth()
  const [pool, setPool] = useState<'all' | 'domestic' | 'global'>('all')
  const [items, setItems] = useState<FeedPostingDto[]>([])
  const [page, setPage] = useState(1)
  const [total, setTotal] = useState(0)
  const [loading, setLoading] = useState(false)
  const [error, setError] = useState(false)
  const sentinelRef = useRef<HTMLDivElement>(null)

  const loadPage = useCallback(async (nextPage: number, reset: boolean) => {
    setLoading(true)
    setError(false)
    try {
      const res = await homeApi.feed({
        page: nextPage,
        page_size: PAGE_SIZE,
        pool: pool === 'all' ? undefined : pool,
      })
      setItems((prev) => (reset ? res.items : [...prev, ...res.items]))
      setTotal(res.total)
      setPage(nextPage)
    } catch {
      setError(true)
    } finally {
      setLoading(false)
    }
  }, [pool])

  useEffect(() => { loadPage(1, true) }, [loadPage])          // pool 변경 시 리셋
  useEffect(() => { ... IntersectionObserver on sentinelRef ... }, ...)

  const groups = useMemo(() => groupByDate(items), [items])    // 오늘/어제/M월 D일

  return (
    <div className="hfeed">
      <aside className="hfeed__left"><HomeLeftColumn /></aside>
      <main className="hfeed__center">
        {/* 필터바, 날짜 그룹 + HomeFeedCard 목록, 로딩 스켈레톤, sentinel, ErrorState/EmptyState */}
      </main>
      <aside className="hfeed__right"><HomeNewsPanel isAuthed={isAuthed} pool={pool} /></aside>
    </div>
  )
}
```

필터바: pool 토글(전체/국내/해외) + 직무 카테고리 퀵 필터. 카테고리 상태 `useState<string | undefined>()`를 `homeApi.feed`의 `category`로 전달하고 변경 시 리셋 로드. 카테고리 후보는 기존 `GET /api/v1/job-categories`(또는 `career/api.ts`의 기존 로더)에서 가져오되, 없으면 대표 카테고리 몇 개를 상수로 둔다(비주얼 스펙 기준).

무한 스크롤: `IntersectionObserver`가 sentinel 노출 시 `!loading && items.length < total`이면 `loadPage(page + 1, false)`. 날짜 그룹핑 헬퍼:

```ts
function dateLabel(iso: string | null): string {
  if (!iso) return '날짜 미상'
  const d = new Date(iso)
  const today = new Date()
  const diffDays = Math.floor((today.setHours(0,0,0,0) - d.setHours(0,0,0,0)) / 86400000)
  if (diffDays <= 0) return '오늘'
  if (diffDays === 1) return '어제'
  return `${d.getMonth() + 1}월 ${d.getDate()}일`
}

function groupByDate(items: FeedPostingDto[]): { label: string; items: FeedPostingDto[] }[] {
  const groups: { label: string; items: FeedPostingDto[] }[] = []
  for (const item of items) {
    const label = dateLabel(item.post_date)
    const last = groups[groups.length - 1]
    if (last && last.label === label) last.items.push(item)
    else groups.push({ label, items: [item] })
  }
  return groups
}
```

- [ ] **Step 2: HomeLeftColumn.tsx — 로그인/게스트 분기**

- `useAuth()`의 `isAuthed`로 분기.
- 로그인: 프로필 카드(nickname, 이니셜 아바타), 이력서 요약(`useResumesState()`의 `activeResume.skills` 칩, "이력서 편집" → `/resume`), 빠른 링크(북마크 `useBookmarks().length`, 최근 본 `useRecentViews().length` → `/jobs` 또는 상세).
- 게스트: 비주얼 스펙의 CTA 카드 (로그인 → `/login`, 회원가입 → `/signup`).

- [ ] **Step 3: HomeFeedCard.tsx — 공고 카드**

props: `{ posting: FeedPostingDto }`. 렌더 필드: company/industry/region 헤더줄, title, 상대 게시일 + `ddayInfo(posting.close_date)` D-day 뱃지, categories 칩, skills 칩 — `posting.match`가 있으면 `match.owned_skills` 포함 여부로 filled/outline 구분, match ring(SVG, `match.rate`), 상세보기(`navigate('/job/' + posting.id)` + `recordView(String(posting.id))`), 북마크 토글(`toggleBookmark(String(posting.id))`). match가 null이면 개인화 요소 렌더 생략.

- [ ] **Step 4: HomeNewsPanel.tsx — 뉴스 탭 + 미니 요약 카드**

- 탭 상태 `useState<NewsSource>('hackernews')`, 탭 전환 시 `homeApi.news(source)` — 이미 받아온 소스는 state에 캐시해 재요청하지 않는다.
- 아이템 렌더: HN(제목/points/comments_count/원문+스레드 링크), GeekNews(제목/링크), GitHub(repo/description/language/stars). 링크는 `target="_blank" rel="noreferrer"`.
- `fetched_at`으로 "N분 전 갱신" 캡션 (dayjs 없이 `Date.now() - Date.parse(fetched_at)` 분 단위 계산). `error` 플래그면 "잠시 후 다시 시도해주세요" 문구, `stale`이면 "이전 캐시" 뱃지 (문구는 비주얼 스펙).
- 미니 요약 카드: `homeApi.postingTimeline(pool==='all'?'domestic':pool)` → 마지막 daily의 `total`("오늘 신규 공고"), 로그인+이력서 시 `matched`("내 스택과 겹치는 공고"); `homeApi.skillShare(...)` top3("수요 Top 3 스킬"). 각 카드 클릭 → `/market` 또는 `/`. 개별 fetch 실패 시 해당 카드만 숨김.

- [ ] **Step 5: DesktopHome.css**

비주얼 스펙의 클래스명/값을 그대로 구현. 반응형: `@media (max-width: 1279px)`에서 우측 컬럼을 중앙 하단으로, `@media (max-width: 1023px)`에서 좌측 컬럼 숨김(또는 상단 요약으로) — 정확한 동작은 비주얼 스펙.

- [ ] **Step 6: 빌드 + 수동 확인**

Run: `cd /home/rivermoon/Documents/techeer-2026-summer-a/frontend && npm run build`
Expected: 성공

- [ ] **Step 7: 커밋 (신규 파일만)**

```bash
cd /home/rivermoon/Documents/techeer-2026-summer-a/frontend
git add src/desktop/pages/home/
git commit -m "feat(home): 홈 피드 3컬럼 화면 추가 (공고 타임라인 + 기술 뉴스 + 미니 요약)"
```

(App.tsx / DesktopShell.tsx는 커밋하지 않는다.)

---

## Task V1: 통합 검증 (Playwright)

**Files:** 없음 (검증 전용)

- [ ] **Step 1: 로컬 스택 기동** — 백엔드(uvicorn + Redis + DB, 기존 docker-compose/기동 방식 사용), 프론트 `npm run dev`.
- [ ] **Step 2: 게스트 시나리오** — 비로그인으로 `/home` 접속: 3컬럼 렌더, 좌측 CTA 카드, 공고 카드에 매치 요소 없음, 뉴스 탭 3개 전환(HN 실데이터 로드 및 Redis `news:hackernews` 키 생성 확인), 미니 카드 수치 표시.
- [ ] **Step 3: 로그인 시나리오** — 로그인 후 `/home`: 좌측 프로필/이력서 요약, 공고 카드 매치율 링 + 보유/미보유 칩 구분, 사이드바 "홈" 활성 상태.
- [ ] **Step 4: 무한 스크롤** — 피드 끝까지 스크롤해 2페이지 이상 로드 확인.
- [ ] **Step 5: 상세 이동** — 카드 상세보기 → `/job/:id` 정상 이동, 북마크 토글 유지 확인.
- [ ] **Step 6: 반응형** — 뷰포트 1440 / 1100 / 900px에서 컬럼 배치 확인.
- [ ] **Step 7: 백엔드 전체 pytest 최종 실행** — `python -m pytest tests/ -q` 전부 PASS.
